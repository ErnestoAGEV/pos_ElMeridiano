# Login con PIN de 4 dígitos — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar el login por email/contraseña por un login con PIN numérico de 4 dígitos, con teclado en pantalla, y agregar la posibilidad de cambiar el PIN desde Personalización.

**Architecture:** Se agrega la columna `pin_hash` a la tabla `usuarios` existente (sistema de un solo usuario). El backend (Electron/IPC) gana dos handlers nuevos (`auth:login-pin`, `auth:cambiar-pin`) que reemplazan a los de password. El frontend reemplaza el formulario de `LoginPage.jsx` por un teclado numérico con indicador de progreso, y `PersonalizacionPage.jsx` gana una sección "Seguridad" para cambiar el PIN.

**Tech Stack:** Electron, better-sqlite3, bcryptjs (ya en el proyecto), React 19, Tailwind CSS, Zustand, react-hot-toast.

**Nota sobre pruebas:** Este proyecto no tiene un framework de pruebas automatizadas configurado (sin `jest`/`vitest` en `package.json`). La verificación de lógica de base de datos/backend se hace con scripts sueltos ejecutados con el runtime de Node embebido en Electron (`ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron.cmd script.cjs`), igual que se hizo en sesiones anteriores de este mismo proyecto. La verificación de UI es manual, corriendo la app con `npm run electron:dev`.

---

## Task 1: Migración de base de datos — columna `pin_hash`

**Files:**
- Modify: `electron/database.cjs` (dentro de `initSchema`, justo antes del comentario `// Migration: add estatus column to ventas`)
- Modify: `electron/database.cjs` (función `seedDefaultUser`)

- [ ] **Step 1: Agregar la migración de columna en `initSchema()`**

En `electron/database.cjs`, busca el comentario `// Migration: add estatus column to ventas` dentro de `initSchema()`. Justo antes de ese comentario (después del último bloque de migraciones de `config_tienda`, el de `mano_obra_plata_fijo`), agrega la migración para `usuarios`:

```javascript
  // Migration: add pin_hash column to usuarios
  const usuariosCols = db.prepare("PRAGMA table_info(usuarios)").all()
  if (!usuariosCols.find(c => c.name === 'pin_hash')) {
    db.exec('ALTER TABLE usuarios ADD COLUMN pin_hash TEXT')
  }
```

- [ ] **Step 2: Sembrar el PIN por defecto en `seedDefaultUser()`**

Reemplaza la función `seedDefaultUser` completa por:

```javascript
function seedDefaultUser() {
  const bcrypt = require('bcryptjs')
  const existing = db.prepare('SELECT id, pin_hash FROM usuarios LIMIT 1').get()
  if (!existing) {
    const passwordHash = bcrypt.hashSync('admin123', 10)
    const pinHash = bcrypt.hashSync('1234', 10)
    db.prepare('INSERT INTO usuarios (nombre, email, password_hash, pin_hash) VALUES (?, ?, ?, ?)').run(
      'Administrador', 'admin@meridiano.com', passwordHash, pinHash
    )
  } else if (!existing.pin_hash) {
    const pinHash = bcrypt.hashSync('1234', 10)
    db.prepare('UPDATE usuarios SET pin_hash = ? WHERE id = ?').run(pinHash, existing.id)
  }
}
```

Esto cubre dos casos: instalación nueva (usuario no existe, se crea ya con PIN) e instalación existente (usuario existe pero sin `pin_hash`, se le asigna el default). Es idempotente: si ya tiene `pin_hash`, no hace nada.

- [ ] **Step 3: Verificar la migración con un script suelto**

Crea un archivo temporal en la raíz del proyecto (se borra en el siguiente paso) llamado `_test_pin_migration_temp.cjs`:

```javascript
const Database = require('better-sqlite3')
const db = new Database(':memory:')

db.exec(`
  CREATE TABLE usuarios (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nombre TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  );
`)

// Simula initSchema: agregar columna pin_hash
const usuariosCols = db.prepare("PRAGMA table_info(usuarios)").all()
if (!usuariosCols.find(c => c.name === 'pin_hash')) {
  db.exec('ALTER TABLE usuarios ADD COLUMN pin_hash TEXT')
}

// Simula seedDefaultUser: caso 1, usuario no existe
const bcrypt = require('bcryptjs')
function seedDefaultUser() {
  const existing = db.prepare('SELECT id, pin_hash FROM usuarios LIMIT 1').get()
  if (!existing) {
    const passwordHash = bcrypt.hashSync('admin123', 10)
    const pinHash = bcrypt.hashSync('1234', 10)
    db.prepare('INSERT INTO usuarios (nombre, email, password_hash, pin_hash) VALUES (?, ?, ?, ?)').run(
      'Administrador', 'admin@meridiano.com', passwordHash, pinHash
    )
  } else if (!existing.pin_hash) {
    const pinHash = bcrypt.hashSync('1234', 10)
    db.prepare('UPDATE usuarios SET pin_hash = ? WHERE id = ?').run(pinHash, existing.id)
  }
}

seedDefaultUser()
const user1 = db.prepare('SELECT * FROM usuarios LIMIT 1').get()
console.log('Caso 1 (usuario nuevo):', user1.pin_hash ? 'PASS: pin_hash poblado' : 'FAIL')
console.log('PIN 1234 valida:', bcrypt.compareSync('1234', user1.pin_hash) ? 'PASS' : 'FAIL')

// Simula seedDefaultUser: caso 2, usuario existente sin pin_hash (reinstalacion)
db.exec('DELETE FROM usuarios')
db.prepare('INSERT INTO usuarios (nombre, email, password_hash, pin_hash) VALUES (?, ?, ?, ?)').run(
  'Administrador', 'admin@meridiano.com', bcrypt.hashSync('admin123', 10), null
)
seedDefaultUser()
const user2 = db.prepare('SELECT * FROM usuarios LIMIT 1').get()
console.log('Caso 2 (usuario existente sin pin_hash):', user2.pin_hash ? 'PASS: pin_hash sembrado' : 'FAIL')

// Simula seedDefaultUser: caso 3, usuario ya tiene pin_hash (no debe tocarlo)
const pinHashOriginal = bcrypt.hashSync('9999', 10)
db.prepare('UPDATE usuarios SET pin_hash = ? WHERE id = ?').run(pinHashOriginal, user2.id)
seedDefaultUser()
const user3 = db.prepare('SELECT * FROM usuarios LIMIT 1').get()
console.log('Caso 3 (pin_hash existente no se sobreescribe):', user3.pin_hash === pinHashOriginal ? 'PASS' : 'FAIL')
```

Ejecuta:

```bash
cd "C:/Users/vevle/OneDrive/Documentos/POS_MERIDIANO"
ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron.cmd _test_pin_migration_temp.cjs
```

Expected: los 4 `console.log` deben decir `PASS`.

- [ ] **Step 4: Borrar el script temporal**

```bash
rm _test_pin_migration_temp.cjs
```

- [ ] **Step 5: Commit**

```bash
git add electron/database.cjs
git commit -m "feat: agregar pin_hash a usuarios con migracion y seed por defecto"
```

---

## Task 2: Backend IPC — `auth:login-pin` y `auth:cambiar-pin`

**Files:**
- Modify: `electron/ipc/auth.cjs` (reemplazo completo)

- [ ] **Step 1: Reemplazar el contenido de `electron/ipc/auth.cjs`**

```javascript
const { ipcMain } = require('electron')
const bcrypt = require('bcryptjs')
const { getDb } = require('../database.cjs')

ipcMain.handle('auth:login-pin', (_event, { pin }) => {
  const db = getDb()
  const user = db.prepare('SELECT * FROM usuarios LIMIT 1').get()
  if (!user || !user.pin_hash) throw new Error('PIN incorrecto')
  const valid = bcrypt.compareSync(String(pin), user.pin_hash)
  if (!valid) throw new Error('PIN incorrecto')
  const { password_hash, pin_hash, ...safeUser } = user
  return safeUser
})

ipcMain.handle('auth:cambiar-pin', (_event, { userId, pinActual, pinNuevo }) => {
  const db = getDb()
  const user = db.prepare('SELECT * FROM usuarios WHERE id = ?').get(userId)
  if (!user) throw new Error('Usuario no encontrado')
  const valid = bcrypt.compareSync(String(pinActual), user.pin_hash)
  if (!valid) throw new Error('PIN actual incorrecto')
  if (!/^\d{4}$/.test(String(pinNuevo))) {
    throw new Error('El PIN debe ser de 4 digitos numericos')
  }
  const hash = bcrypt.hashSync(String(pinNuevo), 10)
  db.prepare('UPDATE usuarios SET pin_hash = ? WHERE id = ?').run(hash, userId)
  return true
})
```

Esto reemplaza por completo los handlers `auth:login` y `auth:cambiar-password` (no hay otro código que los use — ver Task 3).

- [ ] **Step 2: Verificar los handlers con un script suelto**

Crea `_test_auth_pin_temp.cjs` en la raíz del proyecto:

```javascript
const Database = require('better-sqlite3')
const bcrypt = require('bcryptjs')
const db = new Database(':memory:')

db.exec(`
  CREATE TABLE usuarios (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nombre TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    pin_hash TEXT
  );
`)
db.prepare('INSERT INTO usuarios (nombre, email, password_hash, pin_hash) VALUES (?, ?, ?, ?)').run(
  'Administrador', 'admin@meridiano.com', bcrypt.hashSync('admin123', 10), bcrypt.hashSync('1234', 10)
)

// Replica la logica de auth:login-pin
function loginPin(pin) {
  const user = db.prepare('SELECT * FROM usuarios LIMIT 1').get()
  if (!user || !user.pin_hash) throw new Error('PIN incorrecto')
  const valid = bcrypt.compareSync(String(pin), user.pin_hash)
  if (!valid) throw new Error('PIN incorrecto')
  const { password_hash, pin_hash, ...safeUser } = user
  return safeUser
}

// Replica la logica de auth:cambiar-pin
function cambiarPin(userId, pinActual, pinNuevo) {
  const user = db.prepare('SELECT * FROM usuarios WHERE id = ?').get(userId)
  if (!user) throw new Error('Usuario no encontrado')
  const valid = bcrypt.compareSync(String(pinActual), user.pin_hash)
  if (!valid) throw new Error('PIN actual incorrecto')
  if (!/^\d{4}$/.test(String(pinNuevo))) throw new Error('El PIN debe ser de 4 digitos numericos')
  const hash = bcrypt.hashSync(String(pinNuevo), 10)
  db.prepare('UPDATE usuarios SET pin_hash = ? WHERE id = ?').run(hash, userId)
  return true
}

// Caso 1: login con PIN correcto
const user = loginPin('1234')
console.log('Login PIN correcto:', user.nombre === 'Administrador' && !user.pin_hash && !user.password_hash ? 'PASS' : 'FAIL')

// Caso 2: login con PIN incorrecto
try {
  loginPin('0000')
  console.log('Login PIN incorrecto: FAIL (no lanzo error)')
} catch (err) {
  console.log('Login PIN incorrecto:', err.message === 'PIN incorrecto' ? 'PASS' : 'FAIL')
}

// Caso 3: cambiar PIN exitoso
cambiarPin(user.id, '1234', '5678')
const relogueado = loginPin('5678')
console.log('Cambiar PIN + relogin con nuevo PIN:', relogueado ? 'PASS' : 'FAIL')

// Caso 4: cambiar PIN con PIN actual incorrecto
try {
  cambiarPin(user.id, '0000', '1111')
  console.log('Cambiar PIN actual incorrecto: FAIL (no lanzo error)')
} catch (err) {
  console.log('Cambiar PIN actual incorrecto:', err.message === 'PIN actual incorrecto' ? 'PASS' : 'FAIL')
}

// Caso 5: cambiar PIN con formato invalido
try {
  cambiarPin(user.id, '5678', '12a4')
  console.log('Cambiar PIN formato invalido: FAIL (no lanzo error)')
} catch (err) {
  console.log('Cambiar PIN formato invalido:', err.message === 'El PIN debe ser de 4 digitos numericos' ? 'PASS' : 'FAIL')
}
```

Ejecuta:

```bash
cd "C:/Users/vevle/OneDrive/Documentos/POS_MERIDIANO"
ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron.cmd _test_auth_pin_temp.cjs
```

Expected: los 5 `console.log` deben decir `PASS`.

- [ ] **Step 3: Borrar el script temporal**

```bash
rm _test_auth_pin_temp.cjs
```

- [ ] **Step 4: Commit**

```bash
git add electron/ipc/auth.cjs
git commit -m "feat: reemplazar auth por login/cambio de PIN de 4 digitos"
```

---

## Task 3: Preload y servicio de frontend

**Files:**
- Modify: `electron/preload.cjs:4-7`
- Modify: `src/modules/auth/authService.js` (reemplazo completo)

- [ ] **Step 1: Actualizar `electron/preload.cjs`**

Reemplaza el bloque `auth` (líneas 4-7):

```javascript
  auth: {
    loginPin: (data) => ipcRenderer.invoke('auth:login-pin', data),
    cambiarPin: (data) => ipcRenderer.invoke('auth:cambiar-pin', data),
  },
```

- [ ] **Step 2: Reemplazar `src/modules/auth/authService.js`**

```javascript
export async function iniciarSesionConPin(pin) {
  return window.api.auth.loginPin({ pin })
}

export async function cambiarPin({ userId, pinActual, pinNuevo }) {
  return window.api.auth.cambiarPin({ userId, pinActual, pinNuevo })
}
```

- [ ] **Step 3: Commit**

```bash
git add electron/preload.cjs src/modules/auth/authService.js
git commit -m "feat: exponer auth de PIN en preload y authService"
```

---

## Task 4: Animación de error (shake) en Tailwind

**Files:**
- Modify: `tailwind.config.js:4-56`

- [ ] **Step 1: Agregar `keyframes` y `animation` a la config**

En `tailwind.config.js`, dentro de `theme.extend`, después del bloque `boxShadow` (antes del `},` que cierra `extend`), agrega:

```javascript
      keyframes: {
        shake: {
          '0%, 100%': { transform: 'translateX(0)' },
          '20%': { transform: 'translateX(-8px)' },
          '40%': { transform: 'translateX(8px)' },
          '60%': { transform: 'translateX(-6px)' },
          '80%': { transform: 'translateX(6px)' },
        },
      },
      animation: {
        shake: 'shake 0.4s ease-in-out',
      },
```

No borres ni modifiques `fontFamily`, `colors` ni `boxShadow` — el bloque de arriba se inserta después de `boxShadow: { ... }` y antes del `},` que cierra `extend`. Verifica que quede una coma después del `}` que cierra `boxShadow` antes de tu bloque nuevo.
```

- [ ] **Step 2: Commit**

```bash
git add tailwind.config.js
git commit -m "feat: agregar animacion shake para error de PIN"
```

---

## Task 5: Rediseño de `LoginPage.jsx`

**Files:**
- Modify: `src/modules/auth/LoginPage.jsx` (reemplazo completo)

- [ ] **Step 1: Reemplazar el contenido completo de `src/modules/auth/LoginPage.jsx`**

```jsx
import { useState, useEffect, useCallback } from 'react'
import { useNavigate } from 'react-router-dom'
import { Delete } from 'lucide-react'
import toast from 'react-hot-toast'
import { iniciarSesionConPin } from './authService'
import { useAuthStore } from '../../stores/authStore'
import { useTienda } from '../../context/TiendaContext'
import logoDefault from '../../assets/logo-default.png'

const PIN_LENGTH = 4
const KEYPAD_KEYS = ['1', '2', '3', '4', '5', '6', '7', '8', '9', null, '0', 'borrar']

export function LoginPage() {
  const navigate = useNavigate()
  const setUser = useAuthStore((s) => s.setUser)
  const { config } = useTienda()
  const [pin, setPin] = useState('')
  const [error, setError] = useState(false)
  const [verificando, setVerificando] = useState(false)

  const agregarDigito = useCallback((digito) => {
    setError(false)
    setPin((prev) => (prev.length >= PIN_LENGTH ? prev : prev + digito))
  }, [])

  const borrarDigito = useCallback(() => {
    setError(false)
    setPin((prev) => prev.slice(0, -1))
  }, [])

  // Teclado fisico: digitos 0-9 y Backspace
  useEffect(() => {
    function handleKeyDown(e) {
      if (verificando) return
      if (/^[0-9]$/.test(e.key)) {
        agregarDigito(e.key)
      } else if (e.key === 'Backspace') {
        borrarDigito()
      }
    }
    window.addEventListener('keydown', handleKeyDown)
    return () => window.removeEventListener('keydown', handleKeyDown)
  }, [agregarDigito, borrarDigito, verificando])

  // Auto-submit al llegar a PIN_LENGTH digitos
  useEffect(() => {
    if (pin.length !== PIN_LENGTH || verificando) return

    let cancelado = false
    setVerificando(true)

    iniciarSesionConPin(pin)
      .then((user) => {
        if (cancelado) return
        setUser(user)
        navigate('/dashboard')
        toast.success(`Bienvenido, ${user.nombre}`)
      })
      .catch((err) => {
        if (cancelado) return
        setError(true)
        setPin('')
        toast.error(err.message || 'PIN incorrecto')
      })
      .finally(() => {
        if (!cancelado) setVerificando(false)
      })

    return () => { cancelado = true }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [pin])

  return (
    <div className="min-h-screen bg-ivory-100 flex items-center justify-center p-4">
      <div className="w-full max-w-xs">
        <div className="text-center mb-8">
          <img
            src={config.logo_path || logoDefault}
            alt={config.nombre}
            className="w-16 h-16 rounded-2xl object-cover mx-auto mb-4 shadow-primary-md bg-white"
          />
          <h1 className="font-display text-3xl font-bold text-warm-900">{config.nombre || 'Sistema Joyero'}</h1>
          {config.slogan && <p className="text-warm-400 text-xs mt-0.5">{config.slogan}</p>}
          <p className="text-warm-400 text-sm mt-1">Ingresa tu PIN para continuar</p>
        </div>

        {/* Indicador de PIN */}
        <div className={`flex justify-center gap-4 mb-8 ${error ? 'animate-shake' : ''}`}>
          {Array.from({ length: PIN_LENGTH }).map((_, i) => (
            <span
              key={i}
              className={`w-4 h-4 rounded-full border-2 transition-colors ${
                error
                  ? 'bg-red-500 border-red-500'
                  : i < pin.length
                  ? 'bg-warm-900 border-warm-900'
                  : 'bg-transparent border-warm-300'
              }`}
            />
          ))}
        </div>

        {/* Teclado numerico */}
        <div className="grid grid-cols-3 gap-3">
          {KEYPAD_KEYS.map((key, i) => {
            if (key === null) return <div key={`empty-${i}`} />
            if (key === 'borrar') {
              return (
                <button
                  key="borrar"
                  type="button"
                  onClick={borrarDigito}
                  disabled={verificando}
                  className="aspect-square rounded-2xl bg-white border border-ivory-300 text-warm-500 flex items-center justify-center hover:bg-ivory-200 active:scale-95 transition-all disabled:opacity-50"
                >
                  <Delete size={20} />
                </button>
              )
            }
            return (
              <button
                key={key}
                type="button"
                onClick={() => agregarDigito(key)}
                disabled={verificando}
                className="aspect-square rounded-2xl bg-white border border-ivory-300 text-warm-800 text-2xl font-display font-semibold hover:bg-ivory-200 active:scale-95 transition-all disabled:opacity-50"
              >
                {key}
              </button>
            )
          })}
        </div>
      </div>
    </div>
  )
}
```

Notas de implementación:
- El componente usa `Delete` de `lucide-react` como ícono de borrar (ya es dependencia del proyecto — verificar disponibilidad en el paso 2).
- La celda vacía en `KEYPAD_KEYS` (índice 9, entre `9` y `0`) mantiene el grid 3×3 simétrico con la fila `Borrar / 0 / (vacío)`.
- `verificando` deshabilita el teclado durante el round-trip IPC para evitar doble submit; también evita que el listener de teclado físico agregue dígitos durante ese lapso.
- El `useEffect` de auto-submit usa `cancelado` para evitar actualizar estado si el componente se desmonta a media petición (viaja a `/dashboard` y el componente se desmonta).

- [ ] **Step 2: Verificar que `Delete` existe en `lucide-react`**

```bash
cd "C:/Users/vevle/OneDrive/Documentos/POS_MERIDIANO"
node -e "const icons = require('lucide-react'); console.log(typeof icons.Delete)"
```

Expected: `function`. Si en cambio imprime `undefined`, sustituye el import por `Delete as Backspace` fallback: usa `X` (que sí existe, ya usado en `PersonalizacionPage.jsx`) en su lugar, ajustando el import y el uso en el JSX.

- [ ] **Step 3: Probar manualmente la app**

```bash
npm run electron:dev
```

Verifica:
- La pantalla de login muestra el logo/nombre/slogan de la tienda y el teclado numérico con 4 círculos vacíos arriba.
- Tecleando `1`, `2`, `3`, `4` con el mouse en el teclado en pantalla: los círculos se rellenan uno por uno y al 4° dígito hace login automáticamente (PIN por defecto es `1234` en una base de datos nueva) y navega a `/dashboard` con un toast "Bienvenido, Administrador".
- Repite el login (cierra sesión o recarga) y teclea un PIN incorrecto (ej. `0000`): los 4 círculos parpadean en rojo, se limpian, aparece un toast "PIN incorrecto", y puedes reintentar de inmediato.
- Repite usando el teclado físico (dígitos 0-9 y Backspace) en vez del mouse: mismo comportamiento.

- [ ] **Step 4: Commit**

```bash
git add src/modules/auth/LoginPage.jsx
git commit -m "feat: rediseñar login con teclado numerico de PIN"
```

---

## Task 6: Sección "Seguridad" en Personalización (cambiar PIN)

**Files:**
- Modify: `src/modules/personalizacion/PersonalizacionPage.jsx`

- [ ] **Step 1: Agregar imports necesarios**

En `src/modules/personalizacion/PersonalizacionPage.jsx`, el import de íconos actualmente es (línea 2):

```javascript
import { Save, Upload, X, Palette, Type, Store, Database, Download, UploadCloud, FolderOpen, Clock, CheckCircle, Barcode, Coins } from 'lucide-react'
```

Reemplázalo por (agrega `Lock`):

```javascript
import { Save, Upload, X, Palette, Type, Store, Database, Download, UploadCloud, FolderOpen, Clock, CheckCircle, Barcode, Coins, Lock } from 'lucide-react'
```

Justo debajo de los imports existentes (después de `import { ConfirmDialog } from '../../components/ui/ConfirmDialog'`), agrega:

```javascript
import { cambiarPin } from '../auth/authService'
import { useAuthStore } from '../../stores/authStore'
```

- [ ] **Step 2: Agregar estado local para el formulario de PIN**

Dentro de `export function PersonalizacionPage() {`, justo después de la línea `const { config, updateConfig } = useTienda()`, agrega:

```javascript
  const user = useAuthStore((s) => s.user)
  const [pinForm, setPinForm] = useState({ actual: '', nuevo: '', confirmar: '' })
  const [cambiandoPin, setCambiandoPin] = useState(false)
```

- [ ] **Step 3: Agregar el handler de cambio de PIN**

Después de la función `handleSave` existente (justo antes de `async function handleBackup() {`), agrega:

```javascript
  function handlePinChange(field) {
    return (e) => {
      const val = e.target.value.replace(/\D/g, '').slice(0, 4)
      setPinForm((prev) => ({ ...prev, [field]: val }))
    }
  }

  async function handleCambiarPin() {
    if (!/^\d{4}$/.test(pinForm.actual) || !/^\d{4}$/.test(pinForm.nuevo) || !/^\d{4}$/.test(pinForm.confirmar)) {
      toast.error('Los 3 campos deben tener exactamente 4 dígitos.')
      return
    }
    if (pinForm.nuevo !== pinForm.confirmar) {
      toast.error('El PIN nuevo y su confirmación no coinciden.')
      return
    }
    setCambiandoPin(true)
    try {
      await cambiarPin({ userId: user.id, pinActual: pinForm.actual, pinNuevo: pinForm.nuevo })
      toast.success('PIN actualizado.')
      setPinForm({ actual: '', nuevo: '', confirmar: '' })
    } catch (err) {
      toast.error(err.message || 'Error al cambiar el PIN.')
    } finally {
      setCambiandoPin(false)
    }
  }
```

- [ ] **Step 4: Agregar la sección visual "Seguridad"**

En el JSX, después de la sección "Fórmulas de precios" (busca `{/* Section 4: Respaldo automatico */}`) e inmediatamente antes de esa línea, agrega la nueva sección:

```jsx
      {/* Section: Seguridad */}
      <section className="card rounded-xl p-6 space-y-4">
        <div className="flex items-center gap-3">
          <Lock size={20} className="text-warm-600" />
          <h2 className="font-display text-xl text-warm-900">Seguridad</h2>
        </div>

        <p className="text-sm text-warm-600">
          Cambia el PIN de 4 dígitos con el que inicias sesión en el sistema.
        </p>

        <div className="grid grid-cols-1 gap-4 sm:grid-cols-3 max-w-xl">
          <Input
            label="PIN actual"
            type="password"
            inputMode="numeric"
            maxLength={4}
            value={pinForm.actual}
            onChange={handlePinChange('actual')}
            placeholder="••••"
          />
          <Input
            label="PIN nuevo"
            type="password"
            inputMode="numeric"
            maxLength={4}
            value={pinForm.nuevo}
            onChange={handlePinChange('nuevo')}
            placeholder="••••"
          />
          <Input
            label="Confirmar PIN nuevo"
            type="password"
            inputMode="numeric"
            maxLength={4}
            value={pinForm.confirmar}
            onChange={handlePinChange('confirmar')}
            placeholder="••••"
          />
        </div>

        <Button onClick={handleCambiarPin} disabled={cambiandoPin}>
          <Lock size={16} />
          {cambiandoPin ? 'Cambiando...' : 'Cambiar PIN'}
        </Button>
      </section>

      {/* Section 4: Respaldo automatico */}
```

- [ ] **Step 5: Probar manualmente**

```bash
npm run electron:dev
```

Entra con el PIN `1234`, ve a Personalización → sección "Seguridad":
- Cambia el PIN escribiendo `1234` en "PIN actual", `5678` en "PIN nuevo" y "Confirmar PIN nuevo" → toast "PIN actualizado.", campos se limpian.
- Cierra sesión (o recarga la app) y entra con el nuevo PIN `5678` → debe funcionar.
- Vuelve a Personalización → Seguridad e intenta cambiar el PIN con un "PIN actual" incorrecto → toast de error, los campos NO se limpian.
- Intenta con "PIN nuevo" y "Confirmar" distintos → toast de error de validación (no debería llegar a llamar al backend).
- Deja el PIN en `5678` para no romper el flujo, o vuelve a cambiarlo a `1234` si prefieres dejar el ambiente como estaba.

- [ ] **Step 6: Commit**

```bash
git add src/modules/personalizacion/PersonalizacionPage.jsx
git commit -m "feat: agregar cambio de PIN en seccion Seguridad de Personalizacion"
```

---

## Task 7: Actualizar manual de usuario

**Files:**
- Modify: `MANUAL_DE_USUARIO.md:50-76`

- [ ] **Step 1: Reemplazar la sección "2. Inicio de Sesion"**

Reemplaza el bloque completo (líneas 50-76, desde `## 2. Inicio de Sesion` hasta el `---` que le sigue) por:

```markdown
## 2. Inicio de Sesion

Al abrir la aplicacion, veras la pantalla de inicio de sesion con un teclado numerico.

### PIN por defecto

La primera vez que uses el sistema, ingresa con el PIN que viene por defecto:

- **PIN:** `1234`

Puedes cambiarlo despues desde **Personalizacion → Seguridad**.

### Pasos

1. Teclea tu PIN de 4 digitos usando el teclado en pantalla (o el teclado fisico de tu computadora).
2. Al completar el 4to digito, el sistema entra automaticamente — no hay boton "Iniciar sesion".
3. Si el PIN es correcto, veras el mensaje "Bienvenido" y entraras al Dashboard.
4. Si el PIN es incorrecto, los circulos se pondran en rojo y se limpiara la pantalla para que vuelvas a intentar.

---
```

- [ ] **Step 2: Commit**

```bash
git add MANUAL_DE_USUARIO.md
git commit -m "docs: actualizar manual de usuario para login con PIN"
```

---

## Task 8: Verificación final end-to-end

**Files:** Ninguno (solo verificación manual)

- [ ] **Step 1: Probar el flujo completo desde cero**

```bash
npm run electron:dev
```

Checklist:
- [ ] Login con PIN correcto (teclado en pantalla) entra al Dashboard.
- [ ] Login con PIN correcto (teclado físico) entra al Dashboard.
- [ ] Login con PIN incorrecto muestra error visual + toast, y permite reintentar sin recargar la página.
- [ ] Cambiar PIN desde Personalización → Seguridad con PIN actual correcto funciona y el nuevo PIN sirve para el siguiente login.
- [ ] Cambiar PIN con PIN actual incorrecto muestra error y no cambia nada.
- [ ] Cambiar PIN con "nuevo" y "confirmar" distintos muestra error de validación sin llamar al backend.
- [ ] Ya no aparece ningún campo de correo/contraseña en ningún lado de la app.

- [ ] **Step 2: Confirmar que no quedan referencias muertas al login anterior**

```bash
grep -rn "auth:login'" electron/ src/ 2>/dev/null
grep -rn "cambiarPassword\|iniciarSesion\b" src/ electron/ 2>/dev/null
```

Expected: sin resultados (o solo coincidencias dentro de `docs/`, que no se tocan en este plan).

- [ ] **Step 3: Commit final si hubo ajustes**

Si el checklist anterior generó cambios adicionales, commitéalos siguiendo el mismo patrón de mensajes cortos en español usado en el resto del plan.
